{
  "name": "socket.io-tcp-proxy",
  "version": "0.0.3",
  "description": "TCP proxy for socket.io websockets.",
  "main": "bin/release",
  "license": "MIT",
  "bin": {
    "sitp": "./bin/release/main.js"
  },
  "scripts": {
    "start": "npm run release & node ./bin/release/main",
    "debug": "tsc -p tsdebug.json",
    "release": "tsc -p tsconfig.json"
  },
  "author": "Chris Croy",
  "dependencies": {
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^8.0.5",
    "@types/socket.io": "^1.4.31",
    "nodemon": "^1.11.0",
    "ts-node": "^3.1.0",
    "typescript": "^2.4.1"
  }
}
