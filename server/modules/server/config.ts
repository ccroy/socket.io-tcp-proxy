export let ip = '127.0.0.1';
export let domain = 'localhost';
export let port = 8080;
export let protocol = 'http';
export namespace view {
   export let engine = 'hbs';
   export let subDir = 'routes';
}
